<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Open+Sans&display=swap" rel="stylesheet">
    <style>
        /* Estilos Gerais */
        body {
            margin: 0;
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #67bd9a;
            background: linear-gradient(185deg, #13b0a5, #0071a7);
        }
    
        h1 { 
            font-family: 'Playfair Display', serif; 
        }

        /* Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
            background-color: #13b0a5;
        }
        .navbar .logo a {
            text-decoration: none;
            font-size: 1.5rem;
            font-weight: bold;
            color: #f9f2a8;
            display: flex;
            align-items: center;
        }
        .navbar .logo img {
            width: 60px;
            height: auto;
            margin-right: 0.5rem;
        }
        .navbar .nav-links {
            list-style: none;
            display: flex;
            gap: 1rem;
        }
        .navbar .nav-links li a {
            text-decoration: none;
            color: #f9f2a8;
            font-weight: bold;
            padding: 0.5rem 1rem;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        .navbar .nav-links li a:hover {
            background-color: #67bd9a;
        }
        #map-container {
            width: 100%;
            height: 100vh;
        }
        .button-container {
            text-align: center;
            margin: 20px;
        }
        .open-map {
            background-color: #4285F4;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            font-size: 16px;
            border-radius: 5px;
            border: none;
            cursor: pointer;
        }
        .open-map:hover {
            background-color: #357ae8;
        }
        button { 
            text-decoration: none;
            width: 100%; 
            padding: 10px; 
            background-color: #67bd9a; 
            color: #0071a7; 
            border: none; 
            border-radius: 5px; 
            font-size: 16px; 
            font-weight: bold;
            cursor: pointer; 
        }
        button:hover { 
            background-color: #13b0a5; 
        }
        .maps {
            margin: 100px auto;
            padding: 20px;
            max-width: 600px;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        /* Footer */
        footer {
            background-color: #13b0a5;
            color: #f9f2a8;
            padding: 1rem 2rem;
            text-align: center;
        }
        .footer-content {
            display: flex;
            flex-direction: column;
            align-items: center;
        }
        .footer-content p {
            margin: 0.5rem 0;
        }
        .back-button {
            position: absolute;
            top: 110px; 
            left: 20px;
            font-size: 18px;
            font-weight: bold;
            color: #f9f2a8;
            text-decoration: none;
            padding: 10px 15px;
            border-radius: 5px;
            display: flex;
            align-items: center;
            gap: 5px;
            transition: background-color 0.3s;
        }
        .back-button:hover { 
            color: #67bd9a; 
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <a href="/home" class="back-button">
        <i class="fas fa-arrow-left"></i> 
    </a>
    <header>
        <nav class="navbar">
            <div class="logo">
                <a href="#">
                    <img src="{{logoSrc}}" alt="Logo Ponte Yarok">{{logoText}}
                </a>
            </div>
            <ul class="nav-links">
                {{#each navLinks}}
                    <li><a href="{{url}}">{{label}}</a></li>
                {{/each}}
            </ul>
        </nav>
    </header>  
    <div class="maps">
        <h1>{{Mapa}}</h1>
        <div class="button-container">
            <a href="https://www.google.com.br/maps/search/abrigos+de+refugiados/" target="_blank" class="open-map">Abrir no Google Maps</a>
        </div>
    </div>

    <footer>
        <div class="footer-content">
            <p>&copy; {{year}} {{footerText}}</p>
        </div>
    </footer>
</body>
</html>
